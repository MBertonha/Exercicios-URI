#include <iostream>
#include <stdio.h>

using namespace std;

int main () {
    int x,y,z;
    cin >> x;
    cin >> y;
    cin >> z;
    if (x > y)
    if (x > z)
    cout << x << " eh o maior" << endl;
    if (y > x)
    if (y > z)
    cout << y << " eh o maior" << endl;
    if (z > x)
    if (z > y)
    cout << z << " eh o maior" << endl;

return 0;

}
